%h1 Pages
%table.table
  %tr
    %th{scope: 'col'} Title
  - @pages.each do |page|
    %tr
      %td= page.Title
      %td= link_to 'Show', admin_page_path(page)
      %td= link_to 'Edit', edit_admin_page_path(page)
      %td= link_to 'Delete', admin_page_path(page), method: :delete, data: { confirm: 'Are you sure?' }
= link_to 'New page', new_admin_page_path
