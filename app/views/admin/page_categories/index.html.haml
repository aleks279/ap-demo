%h1 Categories
%table.table
  %tr
    %th{scope: 'col'} Name
  - @categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'Show', admin_page_category_path(category)
      %td= link_to 'Edit', edit_admin_page_category_path(category)
      %td= link_to 'Delete', admin_page_category_path(category), method: :delete, data: { confirm: 'Are you sure?' }
= link_to 'New category', new_admin_page_category_path
